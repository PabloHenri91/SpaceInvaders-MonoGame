using System;

namespace Hydra
{
    public enum FontName
    {
        Default,
        SpriteFont
    }

    public enum FontSize
    {
        Default,
        size8 = 8,
        size16 = 16,
        size24 = 24,
        size32 = 32
    }
}
